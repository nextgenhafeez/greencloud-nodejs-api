{
  "name": "node-aws-fargate-terraform-app",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "concurrently": "^5.3.0",
    "config": "^3.3.3",
    "express": "^4.17.1",
    "express-validator": "^6.9.2",
    "gravatar": "^1.8.1",
    "jsonwebtoken": "^8.5.1",
    "mongoose": "^5.11.16",
    "nodemon": "^2.0.7",
    "request": "^2.88.2"
  }
}
// ... (your existing code) ...

// Correct way to get the port in Cloud Run
const PORT = process.env.PORT || 8080; // Use Cloud Run's provided PORT, fallback to 8080 for local dev

app.listen(PORT, () => {
  console.log(`Server started on port ${PORT}`);
});

// ... (rest of your server.js code) ...